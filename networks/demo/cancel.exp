#!/usr/bin/expect

set id [lindex $argv 0]
set from [lindex $argv 1]
set chain_id [lindex $argv 2]

set timeout 30
	spawn ./bnbcli dex cancel -f $id --from $from --chain-id=$chain_id
	expect "Password*"
	send "12345678\r"
interact
